# https://peps.python.org/pep-0517/
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

# https://peps.python.org/pep-0621/
[project]
name = "riap"
description = "package description."
readme = "README.md"
requires-python = ">=3.8, <3.10"
license = { text = "BSD 3-Clause License" }
authors = [
    { email = "elearomy.gros@unibe.ch", name = "Éléa Gros" },
]
dynamic = ["version"]
dependencies = ["openpyxl", "omegaconf", "imagecodecs", "numpy", "Pillow", "scipy", "tqdm", "matplotlib==3.8.0", "pyimagej", "opencv-python==4.5.5.64", "opencv-contrib-python==4.7.0.68", "pytorch-lightning==1.3.5", "torchmetrics==0.6.0", "loguru", "einops", "kornia", "yacs", "pytorch-lightning==1.3.5", "nvidia-ml-py", "jupyter", "joblib", "torchvision", "h5py", "scikit-image", "SimpleITK", "seaborn", "spaces", "huggingface-hub==0.34.3"]

[tool.hatch.version]
path = "src/riap/__init__.py"

[tool.hatch.build.targets.wheel]
only-include = ["src"]
sources = ["src"]

# extras
# https://peps.python.org/pep-0621/#dependencies-optional-dependencies
[project.optional-dependencies]
test = ["pytest>=6.0"]

[project.urls]
homepage = "https://github.com/eleagros/riap"
repository = "https://github.com/eleagros/riap"
